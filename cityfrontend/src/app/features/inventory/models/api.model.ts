/**
 * Enveloppes d'API génériques utilisées par le module inventory.
 *
 * @deprecated (Tour 40ter, 2026-05-09) — la source de vérité est désormais
 * `shared/models/api.model.ts`. Ce fichier est conservé en re-export pour
 * compatibilité avec les imports existants — pourra être supprimé dans un
 * tour ultérieur après migration des imports vers `@app/shared/models/...`.
 */

export * from '../../../shared/models/api.model';
