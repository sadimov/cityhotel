--liquibase formatted sql

--changeset cityprojects:012-insert-initial-data splitStatements:true endDelimiter:;

-- =====================================================
-- INSERTION DES DONNÉES INITIALES
-- Placeholder Tour 5A : fichier vide réservé.
-- À compléter dans un Tour ultérieur avec les données de référence
-- (cf. data/donnees-referentielles.sql, data/roles.sql, data/statuts-taches.sql).
-- =====================================================

-- (aucune instruction pour l'instant)
SELECT 1;
